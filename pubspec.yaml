name: agendantas_app
description: AgendANTAS - App per la gestione dei clown dottori ANTAS
publish_to: 'none'
version: 1.1.3+22

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP e networking
  http: ^1.2.0

  # Storage locale
  shared_preferences: ^2.3.0
  path_provider: ^2.1.4

  # State management
  provider: ^6.1.2

  # UI/UX
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10
  cached_network_image: ^3.4.0

  # Utils
  intl: ^0.19.0
  url_launcher: ^6.3.0

  # Charts per statistiche
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
